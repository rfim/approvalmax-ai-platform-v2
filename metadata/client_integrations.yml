clients:
  approvalmax_demo_client:
    status: active
    display_name: ApprovalMax Demo Client
    enabled_contexts:
      companies:
        schema_version: approvalmax_cdc_v1
        features:
          company_master_data: true
      finance_documents:
        schema_version: approvalmax_cdc_v1
        features:
          purchase_orders: true
          invoice_approval: true
      approval_events:
        schema_version: approvalmax_cdc_v1
        features:
          approval_workflows: true
      subscriptions:
        schema_version: approvalmax_cdc_v1
        features:
          billing_plan_tracking: true
      users:
        schema_version: approvalmax_cdc_v1
        features:
          approver_assignment: true
    dashboards:
      approval_operations:
        status: active
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
    apps:
      approval_operations:
        status: active
        framework: streamlit
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
  client_northwind_procurement:
    status: candidate
    display_name: Northwind Procurement
    enabled_contexts:
      companies:
        schema_version: approvalmax_cdc_v1
        features:
          company_master_data: true
        business_key: company_id
      finance_documents:
        schema_version: approvalmax_cdc_v1
        features:
          purchase_orders: true
          invoice_approval: true
          expense_claims: false
        business_key: document_id
      approval_events:
        schema_version: approvalmax_cdc_v1
        features:
          approval_workflows: true
          delegated_approval: false
        business_key: event_id
      users:
        schema_version: approvalmax_cdc_v1
        features:
          approver_assignment: true
        business_key: user_id
    human_review_required: true
    source_request_id: new_client_procurement_dashboard
    dashboards:
      client_operations:
        status: candidate
        grain: company_id
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        requested_metrics:
        - document_count
        - approval_cycle_time_minutes
        - approval_sla_breach_flag
        - validation_status
        human_review_required: true
    apps:
      client_operations:
        status: candidate
        framework: streamlit
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        human_review_required: true
  client_apex_manufacturing:
    status: candidate
    display_name: Apex Manufacturing
    enabled_contexts:
      companies:
        schema_version: approvalmax_cdc_v1
        features:
          company_master_data: true
        business_key: company_id
      finance_documents:
        schema_version: approvalmax_cdc_v1
        features:
          purchase_orders: true
          invoice_approval: true
          expense_claims: false
        business_key: document_id
      approval_events:
        schema_version: approvalmax_cdc_v1
        features:
          approval_workflows: true
        business_key: event_id
      users:
        schema_version: approvalmax_cdc_v1
        features:
          approver_assignment: true
        business_key: user_id
    human_review_required: true
    source_request_id: new_client_apex_manufacturing
    dashboards:
      client_operations:
        status: candidate
        grain: company_id
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        requested_metrics:
        - document_count
        - approval_cycle_time_minutes
        - approval_sla_breach_flag
        - validation_status
        human_review_required: true
    apps:
      client_operations:
        status: candidate
        framework: streamlit
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        human_review_required: true
  client_quality_evidence_probe:
    status: candidate
    display_name: Quality Evidence Probe
    enabled_contexts:
      companies:
        schema_version: approvalmax_cdc_v1
        features:
          company_master_data: true
        business_key: company_id
      finance_documents:
        schema_version: approvalmax_cdc_v1
        features:
          purchase_orders: true
          invoice_approval: true
          legacy_csv_export: false
        business_key: document_id
    human_review_required: true
    source_request_id: client_quality_evidence_probe
    dashboards:
      client_operations:
        status: candidate
        grain: company_id
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        requested_metrics:
        - document_count
        - approval_cycle_time_minutes
        - approval_sla_breach_flag
        - validation_status
        human_review_required: true
    apps:
      client_operations:
        status: candidate
        framework: streamlit
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        human_review_required: true
  client_croydon_lidl:
    status: candidate
    display_name: Croydon Lidl
    enabled_contexts:
      companies:
        schema_version: approvalmax_cdc_v1
        features:
          company_master_data: true
          multi_entity_reporting: true
        business_key: company_id
      finance_documents:
        schema_version: approvalmax_cdc_v1
        features:
          purchase_orders: true
          invoice_approval: true
          expense_claims: false
        business_key: document_id
      approval_events:
        schema_version: approvalmax_cdc_v1
        features:
          approval_workflows: true
          delegated_approvals: true
        business_key: event_id
      users:
        schema_version: approvalmax_cdc_v1
        features:
          approver_assignment: true
          store_manager_approvals: true
        business_key: user_id
      retail_store_locations:
        schema_version: approvalmax_retail_cdc_v1
        features:
          store_level_spend_reporting: true
          regional_approval_routing: true
        business_key: store_id
    human_review_required: true
    source_request_id: new_client_croydon_lidl
    dashboards:
      client_operations:
        status: candidate
        grain: company_id
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        requested_metrics:
        - document_count
        - approval_cycle_time_minutes
        - approval_sla_breach_flag
        - validation_status
        human_review_required: true
    apps:
      client_operations:
        status: candidate
        framework: streamlit
        source_tables:
        - approvalmax_ai_platform.gold.fact_approval_document_lifecycle
        - approvalmax_ai_platform.monitoring.dashboard_quality_status
        human_review_required: true
