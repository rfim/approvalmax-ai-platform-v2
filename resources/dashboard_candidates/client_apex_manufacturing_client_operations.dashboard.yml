# Candidate resource. Move to resources/ after dashboard SQL and permissions are reviewed.
resources:
  dashboards:
    client_apex_manufacturing_client_operations:
      display_name: "[${bundle.target}] Apex Manufacturing Operations"
      file_path: ../../src/dashboards/client_apex_manufacturing_client_operations.lvdash.json
      warehouse_id: "${var.dashboard_warehouse_id}"
