# Candidate resource. Move to resources/ after dashboard SQL and permissions are reviewed.
resources:
  dashboards:
    client_quality_evidence_probe_client_operations:
      display_name: "[${bundle.target}] Quality Evidence Probe Operations"
      file_path: ../../src/dashboards/client_quality_evidence_probe_client_operations.lvdash.json
      warehouse_id: "${var.dashboard_warehouse_id}"
